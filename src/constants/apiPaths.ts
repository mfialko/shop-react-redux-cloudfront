const BASE_URL = "https://r4x7ybtgm8.execute-api.eu-west-1.amazonaws.com/dev";

const API_PATHS = {
  products: `${BASE_URL}/products`,
  order: `${BASE_URL}`,
  import: `${BASE_URL}/import`,
  bff: `${BASE_URL}`,
  cart: `${BASE_URL}`,
};

export default API_PATHS;
